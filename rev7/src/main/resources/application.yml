server:
  servlet:
    context-path: /rev
  port: 8080
spring:
  mvc:
    view:
      suffix: .jsp
      prefix: /WEB-INF/views/
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL94Dialect
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/rev7
    username: postgres
    password: qwerty
